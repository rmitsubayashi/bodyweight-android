ext {
    def resourceFiles = [
            "common",
            "userExercises"
    ]

    resourcePaths = []
    def ROOT_PATH = "src/main/res/"
    resourceFiles.each {
        file -> resourcePaths += ROOT_PATH + file
    }
}